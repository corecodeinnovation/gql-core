import { Module } from "@nestjs/common";
// Previsto: GraphQLModule (schema-first), resolvers con DataLoader,
// AuthDirective (@auth/@role vía JWT de cci-auth-service), complexity limit.
@Module({ imports: [], controllers: [], providers: [] })
export class AppModule {}
