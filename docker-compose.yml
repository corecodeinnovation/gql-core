services:
  api:
    build: .
    env_file: .env
    ports: ["3000:3000"]
    depends_on: [db]
  db:
    image: postgres:16-alpine
    environment: { POSTGRES_USER: gql, POSTGRES_PASSWORD: gql, POSTGRES_DB: gqlcore }
    volumes: ["gqldb:/var/lib/postgresql/data"]
volumes: { gqldb: {} }
